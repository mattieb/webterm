{
  "name": "webterm",
  "version": "3.0.0",
  "description": "a terminal app that neither sings nor dances",
  "type": "module",
  "main": "app/main.js",
  "build": {
    "appId": "io.mattiebee.webterm"
  },
  "scripts": {
    "build": "tsc && esbuild lib/renderer.js --bundle --format=esm --outfile=app/renderer.js && esbuild lib/preload.js --bundle --format=esm --platform=node --packages=external --outfile=app/preload.js && esbuild lib/main.js --bundle --format=esm --platform=node --packages=external --outfile=app/main.js && esbuild src/index.css --bundle --outfile=app/index.css && cp src/index.html app",
    "build:app": "electron-builder",
    "start": "electron ."
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.5",
    "electron": "^34.2.0",
    "electron-builder": "^26.0.8",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "github:mattieb/node-pty#add-argv0-support",
    "os-locale": "^6.0.2"
  }
}
